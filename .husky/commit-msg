if npx --no -- commitlint --edit $1; then
    exit 0
fi

if pnpm dlx --no -- commitlint --edit $1; then
    exit 0
fi

if bunx --no -- commitlint --edit $1; then
    exit 0
fi

if deno run --allow-read --allow-env --allow-run --allow-sys npm:commitlint --edit $1; then
    exit 0
fi

exit 1
